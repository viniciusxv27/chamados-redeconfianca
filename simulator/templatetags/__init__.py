# Pacote de template tags do simulador.
# As tags/filters ficam em ``simulator_tags.py`` (carregadas via {% load simulator_tags %}).
# Este arquivo é apenas o marcador de pacote — não defina filtros aqui (o Django
# nunca carrega o __init__ como biblioteca de tags).
